global:
  scrape_interval: 5s # High frequency sampling for real-time visualization

scrape_configs:
  - job_name: 'network-metrics-agent'
    static_configs:
      # In a Docker Compose network, containers resolve each other by service name
      - targets: ['collector:2112']